[project]
name = "cybersafe2026"
version = "0.1.0"
description = "Permissioned recon-as-a-service capstone - tier-laddered web reconnaissance pipeline"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32",
    "beautifulsoup4>=4.12",
    "lxml>=5.3",
    "protego>=0.3",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]
